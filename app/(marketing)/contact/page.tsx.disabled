'use client';

import { Container, Card, Title, Text } from '@mantine/core';

export default function ContactPage() {
  return (
    <div className="marketing-context">
      <Container size={1200} px={24} py={80}>
        <Card
          shadow="md"
          padding={48}
          radius={16}
          style={{
            backgroundColor: 'var(--marketing-bg-primary)',
            border: '1px solid rgba(0,0,0,0.08)',
            textAlign: 'center'
          }}
        >
          <Title order={1} style={{ color: 'var(--marketing-text-primary)', marginBottom: '24px' }}>
            Get in Touch
          </Title>
          <Text 
            size="lg" 
            style={{ lineHeight: 1.7, maxWidth: '600px', margin: '0 auto 32px auto', color: 'var(--marketing-text-secondary)' }}
          >
            Ready to transform your product management with AI? Let's discuss how PM33 can help your team achieve PMO-level strategic capabilities.
          </Text>
          
          <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: '1rem' }}>
            <Text size="md" style={{ fontWeight: 600, color: 'var(--marketing-primary)' }}>
              📧 hello@pm33.ai
            </Text>
            <Text size="md" style={{ fontWeight: 600, color: 'var(--marketing-primary)' }}>
              📞 Schedule a Demo
            </Text>
            <Text size="md" style={{ fontWeight: 600, color: 'var(--marketing-primary)' }}>
              📍 San Francisco, CA
            </Text>
          </div>
        </Card>
      </Container>
    </div>
  );
}